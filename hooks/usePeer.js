const { useState, useEffect } = require( 'react' );

const usePeer = () => {
    const [peer, setPeer] = useState( null );
    const [myId, setMyId] = useState( '' );

    useEffect( () => {

    }, [] );
}